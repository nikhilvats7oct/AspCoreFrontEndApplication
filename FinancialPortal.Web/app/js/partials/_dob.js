import $ from 'jquery';
import select2 from 'select2';
import moment from 'moment';
import { keepDropdownBelowSelect } from '../_helpers';

export default () => {
    // There is a better code in _select2.js which overrides this.
};